
# Shared secret the admin panel must send as the `x-admin-token` header.
# Copy this file to .env and set a real value — .env is gitignored.
ADMIN_TOKEN=changeme

# Port for the local admin API server (indexer/src/server.ts).
PORT=4000
